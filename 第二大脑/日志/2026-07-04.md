# 7月4日 学习记录

> 🗓 2026年7月4日 · 星期六

## 今日必须完成（最多3个）
- [x] 搭建网站访客记录系统并部署上线
- [x] 配置 Git 自动推送（服务器→GitHub）
- [ ] Mac 自动 pull 到本地

## 时间块
| 时段 | 任务 | 完成 |
|------|------|------|
| 上午 | 设计 page_visits 表结构，编写 db.js 函数 | ✅ |
| 下午 | 创建 routes/visits.js + server.js 中间件 + 部署 | ✅ |
| 晚上 | 配置服务器 git push，Mac auto-pull（遇权限问题） | ⚠️ |

## 今日终端工作

```bash
node -c db.js routes/visits.js server.js
bash deploy.sh
ssh -i ~/.ssh/id_ed25519 root@182.92.106.159
mkdir -p /var/notes-vault/访客记录
pm2 restart note-flow
curl -s https://wapayton.cn
git config user.name "Payton Wang"
git add 访客记录/ && git commit -m "Update visit stats" && git push
```

## 今日学到的关键点（睡前填）
- **sql.js**：SQLite WASM 实现，`db.prepare() + st.bind() + st.step()` 模式
- **Express 中间件**：访问记录在 session 后、路由前，异步不阻塞响应
- **Git SSH**：服务器需 ssh-keygen + 添加公钥到 GitHub 才能免密 push
- **launchd**：macOS 定时任务，`StartInterval` 设间隔，plist 配置

## 没完成的 & 为什么
- Mac auto-pull 权限错误（launchd 沙箱限制 `/Documents`）

---
**明日最重要的1件事：**
解决 Mac 自动 pull + 实现 IP 地理位置识别
